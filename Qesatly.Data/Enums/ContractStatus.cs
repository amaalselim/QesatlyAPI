namespace Qesatly.Data.Enums
{
    public enum ContractStatus
    {
        Active = 0,          // نشط (Active and ongoing)
        FullyPaid = 1        // تم السداد بالكامل (Fully paid off)
    }
}
